import React from 'react';

function StorySelector() {
    // Add logic to allow the user to select different stories
    return (
        <div>
            <h2>Select Your Story</h2>
            {/* Add story selection options here */}
        </div>
    );
}

export default StorySelector;
